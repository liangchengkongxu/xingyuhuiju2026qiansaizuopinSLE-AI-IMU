# 主控对接说明 · 远距离二进制版（2026-07 最新）

> **重要**：为提升 7m+ 收包率，星闪扫播已改回 **22 字节二进制**（不再是 ASCII 行）。  
> 5m 内若仍用 ASCII 解析会出现数值错乱。

---

## 主控必做

1. 解析 **0xFF 厂商域** 中 **22 字节** 载荷，头 `EB 1A 02`
2. **ADV 与 Scan Response** 都尝试（内容相同）
3. 按 **uptime（偏移 4～5，uint16 LE）+ MAC** 去重
4. 100ms 一帧；广播间隔 10ms，同一 uptime 可能重复多次

---

## 22 字节帧格式

```
EB 1A 02 DEV  uptime(2)  ax(2) ay(2) az(2)  gx(2) gy(2)  roll(2) pitch(2)  mag(2)
```

| 偏移 | 字段 | 类型 | 换算 |
|------|------|------|------|
| 0-1 | 厂商 ID | — | `EB 1A` |
| 2 | 协议 | u8 | `0x02` |
| 3 | 设备号 | u8 | 默认 `0x01` |
| 4-5 | uptime | u16 LE | 毫秒低 16 位 |
| 6-11 | ax,ay,az | i16 LE | ×10 → mg |
| 12-15 | gx,gy | i16 LE | dps |
| 16-19 | roll,pitch | i16 LE | ÷10 → 度 |
| 20-21 | magnitude | u16 LE | ÷100 → g |

---

## 解析代码（C）

```c
int paibing_parse_mfg22(const uint8_t *p, paibing_imu_sample_t *out)
{
    if (p[0] != 0xEB || p[1] != 0x1A || p[2] != 0x02) return -1;
    out->uptime_ms = (uint32_t)(p[4] | (p[5] << 8));
    out->ax_mg = (int16_t)(p[6] | (p[7] << 8)) * 10;
    out->ay_mg = (int16_t)(p[8] | (p[9] << 8)) * 10;
    out->az_mg = (int16_t)(p[10] | (p[11] << 8)) * 10;
    out->gx_dps = (int16_t)(p[12] | (p[13] << 8));
    out->gy_dps = (int16_t)(p[14] | (p[15] << 8));
    out->roll_deg = (int16_t)(p[16] | (p[17] << 8)) / 10.0f;
    out->pitch_deg = (int16_t)(p[18] | (p[19] << 8)) / 10.0f;
    out->magnitude_g = (p[20] | (p[21] << 8)) / 100.0f;
    return 0;
}
```

---

## 日志显示（可选）

若 UI 仍要 ASCII 行，主控本地格式化：

```c
snprintf(line, len, "@%u,A%+d,%+d,%+d,G%+d,%+d,R%+d,P%+d,M%u\n",
    sample.uptime_ms,
    sample.ax_mg/10, sample.ay_mg/10, sample.az_mg/10,
    sample.gx_dps, sample.gy_dps,
    (int)(sample.roll_deg*10), (int)(sample.pitch_deg*10),
    (unsigned)(sample.magnitude_g*100));
```

---

## 与 ASCII 版差异

| 项目 | ASCII 版（已弃用） | **当前** |
|------|-------------------|----------|
| 载荷 | ~50 字节文本 | **22 字节二进制** |
| 5m 内 | 尚可 | 更好 |
| 7m+ | 差 | **明显改善** |
| 解析 | sscanf | **按偏移读 int16** |

蓝牙连接 Notify 仍为 ASCII，不受影响。
